RSpec.describe Rubocop::Digitalhospital do
  it "has a version number" do
    expect(Rubocop::Digitalhospital::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
